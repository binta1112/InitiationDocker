<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Galerie d'images</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>FIRST FAMOUS IMAGES</h1>
  <div class="gallery">
    <% photos.forEach((photo) => { %>
      <div class="photo">
        <img src="<%= photo.url %>" alt="Photo de <%= photo.photographer %>">
        <p>Title: <%= photo.title %></p>
      </div>
    <% }) %>
  </div>
</body>
</html>
